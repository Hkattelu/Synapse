# Copy to .env.staging and fill in securely
JWT_SECRET=change-me
CORS_ORIGIN=http://localhost:5173

# Email (optional; if not provided, server logs emails instead)
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
EMAIL_FROM=
EMAIL_TO=

# Rendering
REMOTION_COMPOSITION_ID=MainComposition
# REMOTION_ENTRY can be overridden if needed

