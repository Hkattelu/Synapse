appId: ai.synapse.desktop
productName: Synapse

directories:
  output: release

asar: true
publish: 'never'

files:
  # Include built web assets and Electron sources
  - dist/**
  - electron/**
  - '!electron/packaging/**'
  - package.json

extraMetadata:
  # Ensure the packaged app points to our Electron entry
  main: electron/main.js

mac:
  category: public.app-category.productivity
  target:
    - target: dmg
      arch:
        - x64
        - arm64
    - target: zip
      arch:
        - x64
        - arm64

win:
  target:
    - target: nsis
      arch:
        - x64

linux:
  category: Utility
  target:
    - target: AppImage
      arch:
        - x64

artifactName: '${productName}-${version}-${os}-${arch}.${ext}'
